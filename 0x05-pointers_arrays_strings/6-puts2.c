#include "main.h"

/**
 * puts2 - print strings
 * starting with the first one
 * @str: funtion parameter
 * Return: 0
 */

void puts2(char *str)

{
	int o;

	for (o = 0 ; str[o] != '\0' ; i++)
	{
		_putchar(str[o]);
		i++;
	}
	_putchar('\n');
}
